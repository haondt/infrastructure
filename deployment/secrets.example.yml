chert:
  host: ...
  ansible_user: ...

marble:
  k3s:
    token: ...

charon:
  repo_password: ...
  gcs:
    bucket: ...
    credentials: ...
  hzsb:
    rclone:
      host: ...
      user: ...
      pass: ...

cert_manager:
  acme_email: &acme_email ...
  cloudflare_email: *acme_email
  cloudflare_api_token: ...

rathole:
  servers:
    chert:
      marble:
        remote_addr: ...
        transport:
          noise:
            remote_public_key: ...
        services:
          haondt:
            token: ...
      aeeo:
        remote_addr: ...
        transport:
          noise:
            remote_public_key: ...
        services:
          haondt:
            token: ...
      gabbro:
        remote_addr: ...
        transport:
          noise:
            remote_public_key: ...
        services:
          haondt:
            token: ...
      basalt:
        remote_addr: ...
        transport:
          noise:
            remote_public_key: ...
        services:
          loki:
            token: ...
          mimir:
            token: ...

gitlab_runner:
  token: ...

ssh:
  private_key: |
    ...
