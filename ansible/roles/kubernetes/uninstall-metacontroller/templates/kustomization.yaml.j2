apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - https://github.com/metacontroller/metacontroller/manifests/production?ref={{ kubernetes.metacontroller.version }}

commonLabels:
  infra.haondt.dev/managed-by: ansible
