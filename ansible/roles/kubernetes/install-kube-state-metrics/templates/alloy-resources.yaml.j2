apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: alloy-access
  namespace: kube-state-metrics
  labels:
    infra.haondt.dev/managed-by: ansible
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ kubernetes.alloy.deployment.namespace }}
      ports:
        - port: 8080
          protocol: TCP
